# AI 协作交互日志（人工智能通识课作业02）
## 基本信息
- 协作工具：豆包 AI + DeepSeek 大模型
- 作业主题：论文导读（基于多模态大语言模型的Web自动化智能体设计）+ DeepSeek Chatbot 代码实现
- 协作时间：2026年3月

## 交互记录（按时间顺序）
### 1. 初始需求确认
**用户提问**：
我需要完成人工智能通识课作业02，要求：
1. 任务一：选一篇近年期刊/会议论文，用大模型生成导读（含手动配图+图注）；
2. 任务二：编写调用DeepSeek API的Chatbot示例代码；
3. 按工程化规范组织代码，提交到GitHub。
我选了《基于多模态大语言模型的Web自动化智能体设计》这篇论文，帮我一步步完成。

**AI 回复核心**：
- 确认论文符合要求（核心期刊、2026年网络首发、AI前沿方向）；
- 规划整体流程：先完成论文导读（结构参考指定链接），再编写Chatbot代码，最后工程化提交；
- 给出项目目录结构：`src/`、`tests/`、`images/`、`.env`、`.gitignore` 等。

### 2. 论文导读协作
**用户提问**：
参考链接（微信公众号）的导读结构是「引言/研究背景/研究方法/实验与结果/讨论/结论」，帮我按这个结构生成《基于多模态大语言模型的Web自动化智能体设计》的导读，告诉我截图位置和插入方式。

**AI 回复核心**：
- 生成完整导读文本，严格对齐参考结构；
- 明确截图位置：论文中「多模态Web智能体工作流图」「交互流程图」「实验对比表」；
- 指导图片保存路径（`images/` 文件夹）和 Markdown 插入语法；
- 补充图注编写规范（贴合论文内容，简洁准确）。

### 3. Chatbot 代码实现协作
**用户提问**：
帮我编写调用DeepSeek API的Chatbot代码，要求：
1. 安全管理API Key（不用硬编码）；
2. 包含异常处理；
3. 可运行，支持多轮对话；
4. 编写单元测试。

**AI 回复核心**：
- 提供完整代码（`src/chatbot_deepseek.py`），使用 `python-dotenv` 管理 `.env` 文件；
- 封装 `deepseek_chatbot` 函数，包含 API 调用、异常捕获；
- 编写单元测试（`tests/test_chatbot.py`），覆盖「返回格式验证」「异常处理验证」；
- 指导依赖安装、运行命令、测试命令。

### 4. 工程化与问题排查协作
**用户提问**：
1. `.env`/`.gitignore` 如何命名？
2. 运行代码提示「Insufficient Balance」（DeepSeek 余额不足）；
3. 单元测试断言失败，如何适配真实场景？

**AI 回复核心**：
- 详细指导 `.env`/`.gitignore` 命名（无后缀、放根目录）；
- 说明「余额不足」是业务问题，代码逻辑本身正确，补充 README 说明即可；
- 修改测试用例，适配真实场景（只验证返回格式，不强制要求 API 调用成功）；
- 指导 Git 提交、推送命令，确保敏感文件不上传。

### 5. 最终整合与提交
**用户提问**：
帮我完善 README.md，整合两个任务的内容，确认所有文件符合提交要求。

**AI 回复核心**：
- 提供完整 README.md 模板，包含论文导读说明、Chatbot 运行步骤、异常说明；
- 验证项目目录结构、文件完整性；
- 给出最终 Git 提交/推送命令，确保仓库内容完整。

## 关键收获（AI 协作总结）
1. 学会用 `python-dotenv` 安全管理 API Key，避免硬编码泄露；
2. 掌握 Python 调用大模型 API 的通用流程（客户端初始化→函数封装→异常处理）；
3. 理解论文导读的结构化编写方法，掌握 Markdown 插入图片+图注的规范；
4. 熟悉 Python 工程化规范（`src/tests` 目录分离、`.gitignore` 配置、单元测试）；
5. 学会适配真实场景调整代码/测试，而非仅追求「理想结果」。