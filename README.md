# 人工智能通识课作业02：论文导读与DeepSeek Chatbot实践
## 项目概述
本项目完成人工智能通识课作业02的两大核心任务：
1. **任务一**：基于《基于多模态大语言模型的Web自动化智能体设计》完成论文导读（大模型生成+手动配图）；
2. **任务二**：编写调用DeepSeek API的Chatbot示例代码，实现工程化封装与测试；
3. 全程通过AI协作完成，记录完整交互流程，按Python工程化规范组织代码并提交至GitHub。

## 任务一：论文导读
### 1. 论文基础信息
- **论文标题**：基于多模态大语言模型的Web自动化智能体设计
- **发表出处**：《计算机科学与探索》，2026年网络首发
- **核心主题**：融合多模态大语言模型的Web自动化智能体设计，解决传统Web自动化工具界面理解能力不足、交互鲁棒性弱的问题

### 2. 论文导读（DeepSeek生成 + 手动润色）
#### 引言
Web自动化是提升人机交互效率的关键技术，传统Web自动化工具依赖人工编写脚本或固定规则，难以适应复杂多变的Web界面与多样化用户需求。随着大语言模型（LLM）与多模态技术的快速发展，具备自主理解、决策与执行能力的Web智能体成为新的研究热点。

本文聚焦于多模态大语言模型在Web自动化中的应用，设计了一套端到端的Web智能体框架，实现从用户自然语言指令到Web页面操作的自动化执行，为高效Web交互与自动化任务提供了新的解决方案。

#### 研究背景
当前Web智能体研究主要面临两大核心挑战：
1. **Web界面理解能力不足**：传统方法多依赖DOM树解析，缺乏对视觉元素（如按钮位置、图标语义）的多模态理解，难以处理复杂布局与动态界面；
2. **交互鲁棒性与泛化性弱**：现有智能体在面对界面变化、元素定位失败、多步骤任务时，稳定性与成功率仍有较大提升空间。

为解决上述问题，本文提出基于多模态大语言模型的Web自动化智能体，融合视觉与文本语义信息，设计更鲁棒的交互流程与执行机制。

#### 研究方法
本文提出的多模态Web自动化智能体核心方法如下：
1. **Web页面结构化建模**：将Web页面解析为结构化元素树，结合视觉截图与DOM节点信息，构建多模态界面表示；
2. **多模态语义融合**：利用多模态大语言模型，同时处理用户指令文本与Web界面视觉信息，统一理解界面元素、操作意图与执行逻辑；
3. **逐指令上下文感知**：设计逐指令执行机制，维护对话上下文与操作历史，避免多步骤任务的上下文丢失；
4. **交互闭环与容错机制**：构建「指令解析→元素定位→动作执行→反馈采集」闭环流程，引入定位器优先级选择与失败重试策略。

![图1 多模态Web智能体的单步工作流](images/fig1_workflow.png)
*图1 展示了本文Web智能体的单步工作流程，核心包含Web页面结构化建模、多模态语义融合、逐指令上下文感知三大模块，实现从用户指令到自动化执行的端到端链路。*

#### 实验与结果
##### 数据集
实验采用公开Web自动化评测数据集（Mind2Web、WebArena等），覆盖信息查询、表单填写、数据爬取等典型任务。

##### 对比结果
本文方法与4种主流Web智能体（AutoGPT、WebVoyager等）对比，核心结果：
- 平均任务完成率达88.6%，较基线方法提升9%-15%；
- 干扰场景下成功率保持82%以上，显著优于纯文本解析模型；
- 单步指令平均执行时间3-5秒，满足实时交互需求。

![图2 不同方法任务完成率对比](images/fig2_interaction.png)
*图2 对比了本文方法与基线模型在不同任务类型下的完成率，本文方法在各类任务中均取得最优或次优结果。*

#### 讨论
本文方法验证了多模态大语言模型在Web自动化中的潜力，但仍存在局限：
1. 对高度动态、异步加载的Web页面处理能力待提升；
2. 大模型调用成本较高，难以大规模并发部署；
3. 跨域数据集泛化性能需优化。

未来可结合轻量化模型、边缘计算降低成本，提升实时性。

#### 结论
本文提出的多模态Web智能体实现了Web界面的自主理解与高效交互，显著提升了自动化任务的成功率与鲁棒性，为大模型在自动化交互领域的应用提供了新思路，在智能办公、自动化测试等场景具备实用价值。

### 3. 配图说明
- 配图来源：从论文PDF中手动截取核心图表；
- 配图数量：3张（工作流图、交互流程图、实验对比表）；
- 图注编写：基于论文内容，简洁说明图表核心信息，符合学术规范。

## 任务二：DeepSeek Chatbot实现
### 1. 技术选型
- **API平台**：DeepSeek官方API（兼容OpenAI接口）；
- **开发语言**：Python 3.8+；
- **核心依赖**：`openai`（API调用）、`python-dotenv`（敏感信息管理）；
- **工程规范**：`src/` 存放核心代码，`tests/` 存放单元测试，`.gitignore` 保护敏感文件。

### 2. 快速开始
#### （1）环境准备
```bash
# 安装依赖包
pip install openai python-dotenv